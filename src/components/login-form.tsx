import React from "react";
import { Card, CardContent, CardHeader } from "./ui/card";

export default function LoginForm() {
  return (
    <div>
      <Card>
        <CardHeader>Login into Codeforces</CardHeader>

        <CardContent></CardContent>
      </Card>
    </div>
  );
}
